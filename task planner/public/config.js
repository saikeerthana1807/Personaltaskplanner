// Optional: add a hosted backend URL here when you deploy server.js online.
// If this is empty, the app still works, but data is stored privately in this browser.
// Example: window.TASK_API_BASE_URL = "https://your-task-planner-api.onrender.com";
window.TASK_API_BASE_URL = "";
